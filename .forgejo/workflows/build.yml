name: Build and Publish

on:
  push:
    branches:
      - master
    paths-ignore:
      - "README.md"
      - "docker-compose.yml"
      - "LICENSE"

  schedule:
    # Every Sunday at 00:00 UTC
    - cron: "0 0 * * 0"

  workflow_dispatch:

env:
  IMAGE: docker.io/quackerd/sonaive

jobs:
  docker:
    runs-on: linux-amd64
    container:
      image: docker:27-cli

    steps:
      - name: Install Node and Docker CLI
        run: |
          apk add --no-cache \
                      nodejs \
                      npm \
                      git \
                      bash \
                      ca-certificates \
                      docker-cli \
                      docker-cli-buildx

      - uses: actions/checkout@v4

      - name: Set date tag
        id: vars
        run: |
          set -euo pipefail
          echo "date_tag=$(date -u +%Y%m%d)" >> "$GITHUB_OUTPUT"

      - uses: docker/login-action@v3
        with:
          registry: docker.io
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_TOKEN }}


      - name: Build and Publish Slim
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          target: sonaive
          tags: |
            ${{ env.IMAGE }}:latest
            ${{ env.IMAGE }}:weekly-${{ steps.vars.outputs.date_tag }}

      - name: Build and Publish Full
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          target: sonaive-full
          tags: |
            ${{ env.IMAGE }}:latest-full
            ${{ env.IMAGE }}:weekly-${{ steps.vars.outputs.date_tag }}-full

      - name: Clean Up
        if: always()
        run: docker system prune -af --volumes